name: Shared Obsidian Plugin CI

on:
  workflow_call:
    inputs:
      plugin-id:
        required: true
        type: string

jobs:
  test-reusable:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Reusable workflow for ${{ inputs.plugin-id }} is working"
